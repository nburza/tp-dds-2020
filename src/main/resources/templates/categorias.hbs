{{#partial "contenido"}}

    <div>
        <h2>Organizacion {{idOrganizacion}}</h2>
        <a class="btn btn-primary" href="/categorias/nueva" role="button">Nueva Categoria</a>
        <table class="table table-striped table-hover" style="width:100%">
            <thead class="thead-dark">
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Nombre</th>

            </tr>
            </thead>
            {{#each categorias}}
                <tr>
                    <td>{{id}} </td>
                    <td>{{nombre}}</td>
                </tr>
            {{/each}}
        </table>
    </div>

{{/partial}}

{{>layout.hbs}}