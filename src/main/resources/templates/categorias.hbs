{{#partial "contenido"}}
    <h1 class="text-center">Categorias</h1>
    <div>
        <h2>Categorias - Organizacion {{idOrganizacion}}</h2>
        <a class="btn btn-primary" href="/categorias/nueva" role="button">Cargar Categoría</a>
        <table class="table table-striped table-hover" style="width:100%">
            <thead class="thead-dark">
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Nombre</th>

            </tr>
            </thead>
            {{#each categorias}}
                <tr>
                    <td>{{id}} </td>
                    <td>{{nombre}}</td>
                </tr>
            {{/each}}
        </table>
    </div>

{{/partial}}

{{>layout.hbs}}